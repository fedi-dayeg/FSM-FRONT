import { DashbordRouting.Module } from './dashbord-routing.module';

describe('DashbordRouting.Module', () => {
  it('should create an instance', () => {
    expect(new DashbordRouting.Module()).toBeTruthy();
  });
});
